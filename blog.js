const initialState = {

}

const reducer = (state, action) => {
  switch (action.type) {
    default:
      return state
  }
}

const store = Redux.createStore(reducer, initialState)

const blog = () => {

}

store.subscribe(blog)
